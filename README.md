# SIREC-B II — Proyecto Completo

## Ejecutar en Windows:
1. Base de datos: Crea `sirec_b_db` en PostgreSQL y ejecuta `database/schema_sirec_b.sql`.
2. Backend: `cd backend` -> `npm install` -> `npm start`
3. Frontend: `cd frontend` -> `npm install` -> `npm start`
